#ifndef TRIG_H__
#define TRIG_H__

#include <cmath>
#define _USE_MATH_DEFINES

namespace nmsTrig
{

double radians (double d);
double degrees (double r);
float cos (double x );
float sin (double x );

};
#endif